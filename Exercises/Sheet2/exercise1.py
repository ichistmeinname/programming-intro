# Die in der Vorlesung definierte Stackmaschine verhält sich für das `if_then_else` anders, als wir es in der Vorlesung bei der Semantik von `if_then_else` definiert haben. Auch die Semantik von Ausdrücken in Tabellenkalkulationen oder Python unterscheidet sich von der der Stackmaschine.
#
# Um dies zu verstehen, analysieren Sie die jeweilige Semantik des Ausdrucks
#
# ~~~
# if_then_else(x != 0, y / x, 42)
# ~~~
#
# für unterschiedliche Belegungen der verwendeten Variablen. Charakterisieren Sie die Unterschiede.
